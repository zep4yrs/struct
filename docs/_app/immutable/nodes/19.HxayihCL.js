import{$ as e,C as t,H as n,Q as r,T as i,tt as a}from"../chunks/CKVVjNg5.js";import"../chunks/xihTtKlq.js";import{n as o,r as s,t as c}from"../chunks/DpWxwMms.js";import{r as l}from"../chunks/-v8pTM6Z.js";var u=[{type:`choose-next`,stepIndex:1,prompt:`视图（VIEW）在数据库中保存的是什么？`,options:[`一条 SELECT 查询定义`,`一份数据的副本`,`一张物理表`,`一个索引结构`],correctAnswer:`一条 SELECT 查询定义`,hint:`视图本身不占存储空间`,explanation:`视图是虚拟表：数据库只保存 CREATE VIEW 中的 SELECT 定义，不复制任何数据。查询视图时实时执行这条查询。`},{type:`choose-next`,stepIndex:2,prompt:`查询视图时，数据库实际执行的是什么？`,options:[`视图保存的 SELECT 语句`,`直接读取视图的存储数据`,`重建基表`,`刷新索引`],correctAnswer:`视图保存的 SELECT 语句`,hint:`视图没有自己的数据`,explanation:`视图不缓存数据。每次查询视图，数据库都现场执行视图保存的 SELECT，所以结果与基表当前数据完全一致。`},{type:`choose-next`,stepIndex:3,prompt:`视图的主要优点不包括？`,options:[`加快查询速度`,`简化复杂查询`,`隐藏敏感列`,`提供逻辑独立性`],correctAnswer:`加快查询速度`,hint:`视图是逻辑概念，不涉及物理存储优化`,explanation:`视图的优点是把复杂查询封装复用、通过投影隐藏敏感列、基表结构变化时应用层不受影响；但它不加速查询——每次访问仍要执行底层 SELECT。`}],d=[{name:`筛选视图`,description:`WHERE 条件封装为视图`,sql:`CREATE VIEW v_优秀成绩 AS SELECT 学号, 姓名, 成绩 FROM 学生 WHERE 成绩 >= 85`},{name:`隐藏列视图`,description:`只暴露部分列，隐藏敏感字段`,sql:`CREATE VIEW v_学生公开 AS SELECT 学号, 姓名, 专业 FROM 学生`},{name:`聚合视图`,description:`GROUP BY + COUNT 封装为视图`,sql:`CREATE VIEW v_专业人数 AS SELECT 专业, COUNT(*) FROM 学生 GROUP BY 专业`},{name:`连接视图`,description:`两表 JOIN 封装为视图`,sql:`CREATE VIEW v_选课成绩 AS SELECT 学生.姓名, 选课.成绩 FROM 学生 JOIN 选课 ON 学生.学号 = 选课.学号`}],f=class extends c{name=`视图创建与使用`;renderType=`sql-table`;practiceQuestions=u;demoScript=[{type:`compare`,narration:`视图定义只保存查询语句，不保存数据。查询视图时，数据库实时执行这条 SELECT。`},{type:`recurse-enter`,narration:`底层 SELECT 按 FROM → WHERE → SELECT 投影的顺序执行，逐步得到视图的结果集。`},{type:`complete`,narration:`视图完成：基表数据变化会自动反映到视图结果中。视图用于简化查询、隐藏敏感列、提供逻辑独立性。`}];_tables={};presets=d.map(e=>({name:e.name,description:e.description}));customConfig={title:`自定义视图`,fields:[{key:`sql`,label:`SQL 语句`,type:`textarea`,placeholder:`例如：CREATE VIEW v_优秀 AS SELECT 学号, 姓名, 成绩 FROM 学生 WHERE 成绩 >= 85`,default:`CREATE VIEW v_优秀 AS SELECT 学号, 姓名, 成绩 FROM 学生 WHERE 成绩 >= 85`}]};applyPreset(e){let t=d.find(t=>t.name===e);t&&this.init({sql:t.sql,tables:this._tables})}applyCustom(e){let t=(e.sql??``).trim();if(t.length===0)throw Error(`SQL 不能为空`);this.init({sql:t,tables:this._tables})}_parse(e){let t=e.replace(/\s+/g,` `).trim().match(/^CREATE\s+VIEW\s+([\u4e00-\u9fa5\w]+)\s+AS\s+(SELECT[\s\S]+)$/i);if(!t)throw Error(`仅支持 CREATE VIEW 视图名 AS SELECT ... 语句`);let n=t[1],r=t[2].trim();if(r.length===0)throw Error(`视图定义缺少 SELECT 查询`);return{viewName:n,selectSql:r}}init(e){let{sql:t,tables:n}=e;this._tables=n;let r=this._parse(t),i=new l;i.init({sql:r.selectSql,tables:n});let a=i.steps;this.pseudocode=[`CREATE VIEW ${r.viewName} AS SELECT ...`,...i.pseudocode.map(e=>`  ${e}`),`SELECT * FROM ${r.viewName} → 执行保存的查询`],this.steps=[],this._stepId=0;let o=Object.keys(n)[0];this._emit(`init`,`CREATE VIEW ${r.viewName} AS ${r.selectSql}。视图不复制数据，只把这条 SELECT 定义保存到数据字典。`,n[o]??{columns:[],rows:[]},[],[{type:`compare`,indices:n[o]?.rows.map((e,t)=>t)??[]}],0,`CREATE VIEW 把一段 SELECT 查询保存为一个逻辑表（视图）。视图名 ${r.viewName} 像真实表一样可以被查询，但本身不存放任何数据。`),this._emit(`compare`,`基表：数据真实存放在 ${Object.keys(n).join(`、`)} 中。视图只是 ${r.viewName} 的一扇“窗口”，不占存储。`,n[o]??{columns:[],rows:[]},[],[{type:`compare`,indices:n[o]?.rows.map((e,t)=>t)??[]}],1,`视图与基表的区别：基表存数据，视图存查询。查询视图时，相当于把这段 SELECT 现场执行一遍。`);for(let e of a)this._emit(e.type,`视图查询执行：${e.description}`,e.table??{columns:[],rows:[]},e.data,e.highlights,e.pseudocodeLine+1);let s=a[a.length-1]?.table??{columns:[],rows:[]};this._emit(`recurse-exit`,`视图 ${r.viewName} 创建完成：数据字典记录“${r.viewName} → ${r.selectSql}”。视图逻辑结果 ${s.rows.length} 行。`,s,[],[{type:`compare`,indices:s.rows.map((e,t)=>t)}],this.pseudocode.length-1,`视图创建完成。此后可以把 ${r.viewName} 当作表使用：SELECT * FROM ${r.viewName}。`),this._emit(`recurse-enter`,`查询视图：SELECT * FROM ${r.viewName} —— 数据库现场执行保存的查询，返回 ${s.rows.length} 行。`,s,[],[{type:`compare`,indices:s.rows.map((e,t)=>t)}],this.pseudocode.length-1,`每次查询视图都会重新执行底层 SELECT，因此视图结果永远与基表当前数据一致。`);let c=this._updateBaseAndRefresh(r,n);this._emit(`swap`,`基表更新：往 ${o} 插入新记录 ${this._fmtRow(c.added)}。视图不改变，但再查询时自动包含新行（结果 ${c.view.rows.length} 行）。`,c.view,[],[{type:`current`,indices:c.newIndices}],this.pseudocode.length-1,`视图的动态性：基表发生 INSERT / UPDATE / DELETE 后，视图结果随之下一次查询自动更新——因为视图每次都重新执行保存的 SELECT。`),this._emit(`complete`,`演示结束：视图 = 保存的查询，不存数据、随基表动态更新。适合：简化复杂查询、隐藏敏感列、应用层逻辑独立。`,c.view,[],[{type:`compare`,indices:c.view.rows.map((e,t)=>t)}],this.pseudocode.length-1,`视图三大优点：① 简化查询——复杂 JOIN/聚合封装一次、到处复用；② 安全性——只暴露需要的列；③ 逻辑独立性——基表结构调整时应用无感。`),this.totalSteps=this.steps.length}_updateBaseAndRefresh(e,t){let n=Object.keys(t)[0],r=t[n],i=r.columns,a=Math.max(...r.rows.map(e=>Number(e[0])||0)),o=[a+1,`新同学`,`软件工程`,88].slice(0,i.length),s={columns:[...r.columns],rows:[...r.rows.map(e=>[...e]),[...o]]},c=new l;c.init({sql:e.selectSql,tables:{...t,[n]:s}});let u=c.steps[c.steps.length-1]?.table??{columns:[],rows:[]};return{view:u,added:o,newIndices:u.rows.map((e,t)=>({r:e,i:t})).filter(({r:e})=>e.includes(a+1)).map(({i:e})=>e)}}_fmtRow(e){return`(${e.join(`, `)})`}_emit(e,t,n,r,i,a,o){this.steps.push({id:this._stepId++,type:e,description:t,data:[...r],highlights:i,pseudocodeLine:a,presenterNote:o,table:{columns:[...n.columns],rows:n.rows.map(e=>[...e])}})}},p=i(`<b>CREATE VIEW</b> 把一条 SELECT 查询保存为<b>虚拟表</b>（视图）：视图本身不存数据，只存查询定义。 查询视图时数据库现场执行保存的 SELECT，因此<b>基表数据变化会立即反映到视图结果</b>。 逐步播放，看视图从定义、底层查询执行、到基表更新后自动刷新的完整过程。`,1);function m(i,c){e(c,!0);let l={学生:{columns:[`学号`,`姓名`,`专业`,`成绩`],rows:[[20101,`张三`,`计算机`,88],[20102,`李四`,`软件工程`,92],[20103,`王五`,`计算机`,76],[20104,`赵六`,`网络工程`,85],[20105,`孙七`,`软件工程`,63],[20106,`周八`,`计算机`,95]]},选课:{columns:[`学号`,`课程号`,`成绩`],rows:[[20101,`C001`,90],[20101,`C002`,85],[20102,`C001`,95],[20103,`C002`,78],[20105,`C001`,60]]}};function u(e){let t=new f;return t.init({sql:e,tables:l}),t}let m=n(u(d[0].sql));s(i,{sectionNum:`§06`,sectionName:`索引与视图`,title:`视图创建与使用`,desc:e=>{var n=p();a(5),t(e,n)},children:(e,t)=>{o(e,{get engine(){return m},topicId:`view`,topicName:`视图创建与使用`})},$$slots:{desc:!0,default:!0}}),r()}export{m as component};