import{E as e,U as t,et as n,rt as r,tt as i,w as a}from"../chunks/BeWIKSww.js";import"../chunks/xihTtKlq.js";import{t as o}from"../chunks/CyYJNVap.js";import{n as s,t as c}from"../chunks/DQUFm6-3.js";var l=[`-- 主从复制三线程`,`主库: 事务提交 → 写 binlog → dump 线程推送`,`从库: IO 线程接收 → 写 relay log（中继日志）`,`从库: SQL 线程重放 relay log → 数据一致`,`-- 异步复制（默认）/ 半同步（至少一从确认）/ 组复制`],u=[{type:`choose-next`,stepIndex:3,prompt:`relay log（中继日志）的作用是？`,options:[`从库暂存主库发来的 binlog 事件，等待 SQL 线程重放`,`主库的事务日志`,`崩溃恢复日志`,`缓存查询结果`],correctAnswer:`从库暂存主库发来的 binlog 事件，等待 SQL 线程重放`,hint:`IO 与 SQL 两线程的解耦缓冲`,explanation:`IO 线程只负责收（快），SQL 线程负责重放（可能慢）——relay log 作为解耦缓冲区，让两边速度解耦。`},{type:`choose-next`,stepIndex:6,prompt:`默认的异步复制可能出现什么现象？`,options:[`从库读到稍旧的数据（主从延迟）`,`主库数据丢失`,`从库永远追不上`,`事务会失败`],correctAnswer:`从库读到稍旧的数据（主从延迟）`,hint:`异步=不等待确认`,explanation:`异步复制下主库提交不等从库确认，从库重放存在延迟。写后立即读从库可能读到旧值——半同步/组复制可缓解。`}],d=[`客户端写主库`,`binlog`,`dump 线程`,`IO 线程`,`relay log`,`SQL 线程`,`从库数据`],f=[``,`逻辑变更事件`,`推送到从库`,`接收并落盘`,`中继缓冲`,`按序重放`,`最终一致`],p=class extends o{name=`主从复制`;renderType=`graph`;pseudocode=l;practiceQuestions=u;demoScript=[{type:`init`,narration:`MySQL 主从复制：主库写 binlog，dump 线程推送，从库 IO 线程收进 relay log，SQL 线程重放——读写分离与高可用的基础。`},{type:`compare`,narration:`当前阶段：观察事件如何一步步流向从库。`},{type:`edge-select`,narration:`relay log 解耦了接收与重放的速度差。`},{type:`complete`,narration:`复制完成。主从延迟是异步复制的固有现象——半同步复制要求至少一个从库确认后才返回提交成功。`}];presets=[{name:`标准异步复制`,description:`一主一从三线程`}];customConfig={title:`复制演示`,fields:[]};applyPreset(e){this.init([0])}applyCustom(e){this.init([0])}init(e){this.steps=[],this._stepId=0;let t=d.length,n=[`业务事务在主库执行并提交。`,`提交时事件写入 binlog（Row 格式最常用）。`,`主库 dump 线程监听 binlog 变化，推送给订阅的从库。`,`从库 IO 线程接收事件，写入本地 relay log。`,`relay log 按序暂存事件——IO 与 SQL 线程在此解耦。`,`SQL 线程读取 relay log，按序重放变更。`,`重放完成，从库数据与主库最终一致。`],r=e=>{let n={};for(let r=0;r<t;r++)n[r]=r===e?`current`:r<e?`done`:`unvisited`;return{nodes:d.map((e,t)=>({id:t,label:e})),edges:Array.from({length:t-1},(e,t)=>({from:t,to:t+1})),directed:!0,layout:`chain`,nodeState:n,nodeNote:Object.fromEntries(f.map((e,t)=>[t,e]))}};this._emit(`init`,`主从复制全链路：主库三件套（binlog/dump）+ 从库三件套（IO/relay/SQL）。`,r(0),0);for(let e=0;e<t;e++){let t=e===4?`edge-select`:`compare`;this._emit(t,d[e]+`：`+n[e],r(e),Math.min(e+1,l.length-1))}this._emit(`complete`,`复制链路完成。读写分离时注意主从延迟；高可用方案（MGR/MHA）都构建在这套复制之上。`,r(t-1),5),this.totalSteps=this.steps.length}_emit(e,t,n,r){this.steps.push({id:this._stepId++,type:e,description:t,data:[],highlights:[],pseudocodeLine:r,graph:n})}},m=e(`<span class="mono">binlog → dump → IO → relay log → SQL</span> 三线程链路——读写分离与高可用的基础。`,1);function h(e,o){i(o,!0);function l(){let e=new p;return e.init([0]),e}let u=t(l());s(e,{sectionNum:`§10`,sectionName:`高可用`,title:`主从复制`,desc:e=>{var t=m();r(),a(e,t)},children:(e,t)=>{c(e,{get engine(){return u},topicId:`replication`,topicName:`主从复制`})},$$slots:{desc:!0,default:!0}}),n()}export{h as component};