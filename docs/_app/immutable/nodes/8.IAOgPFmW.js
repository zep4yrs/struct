import{E as e,U as t,et as n,tt as r,w as i}from"../chunks/DcYFaMZl.js";import"../chunks/xihTtKlq.js";import{n as a,t as o}from"../chunks/CK0aGdLp.js";import{t as s}from"../chunks/CnDdTnmb.js";var c=class extends s{name=`执行计划与索引选择`;renderType=`sql-table`;pseudocode=[`优化器生成候选执行计划`,`  计划 A：全表扫描（顺序读取每一行）`,`  计划 B：索引查找（B+ 树定位 + 回表）`,`估算每个计划的代价`,`选择代价最小的计划执行`];practiceQuestions=[{type:`choose-next`,stepIndex:1,prompt:`全表扫描的代价主要取决于？`,options:[`表中的总行数`,`查询条件个数`,`索引数量`,`列的数量`],correctAnswer:`表中的总行数`,hint:`扫描要读取每一行`,explanation:`全表扫描需要顺序读取全部数据页，代价与表的总行数成正比；数据越多扫描越慢。`},{type:`choose-next`,stepIndex:3,prompt:`等值查询（WHERE 学号 = 20103）走索引的优势是？`,options:[`只需沿 B+ 树定位少量行，避免全表扫描`,`索引可以修改数据`,`索引不需要存储空间`,`等值查询不能走索引`],correctAnswer:`只需沿 B+ 树定位少量行，避免全表扫描`,hint:`B+ 树查找是 O(log n)`,explanation:`B+ 树索引把查找从"扫描全部行"变成"沿树定位"，命中行数极少时代价远低于全表扫描；但选择性差（如性别列）时索引反而低效。`}];demoScript=[{type:`init`,narration:`优化器根据统计信息为查询生成候选执行计划，并选择代价最小的一个。`},{type:`compare`,narration:`估算候选计划：读取的行数与代价。`},{type:`recurse-enter`,narration:`对比各计划代价，选择最优执行方案。`},{type:`complete`,narration:`执行选中的计划并返回结果。索引不是越多越好——写多读少的表要考虑维护成本。`}];presets=[{name:`等值查询（索引胜）`,description:`WHERE 学号 = 20103：索引查找 1 行`},{name:`范围查询（索引胜）`,description:`WHERE 成绩 >= 85：索引定位后扫描 3 行`},{name:`无索引列查询`,description:`WHERE 专业 = 计算机：全表扫描 5 行`}];customConfig={title:`自定义查询`,fields:[{key:`sql`,label:`SQL 语句`,type:`textarea`,placeholder:`SELECT * FROM 学生 WHERE 学号 = 20103`,default:`SELECT * FROM 学生 WHERE 学号 = 20103`}]};_table={columns:[],rows:[]};_indexedCols=[];init(e){this._table=e.table,this._indexedCols=e.indexedCols,this.steps=[],this._stepId=0,this.totalSteps=0,this.playbackPos=0;let t=this._table.columns,n=this._table.rows,r=this._parseWhere(e.sql);if(!r)throw Error(`仅支持 WHERE 单条件查询，如：SELECT * FROM 学生 WHERE 学号 = 20103`);let i=r.col,a=t.indexOf(i);if(a<0)throw Error(`条件列 `+i+` 不存在`);let o=r.op,s=r.val,c=n.filter(e=>this._evalCond(e[a],o,s)),l=c.length;this._emit(`init`,`查询：SELECT * FROM 学生 WHERE `+i+` `+o+` `+s,{columns:[...t],rows:n.map(e=>[...e])},[],[]);let u=this._indexedCols.includes(i),d=n.length,f=o===`=`?d:Math.ceil(d*.8);if(this._emit(`compare`,`候选计划 A：全表扫描 —— 顺序读取全部 `+d+` 行，逐行判断条件。`,{columns:[`计划`,`读取行数`,`估算代价`,`说明`],rows:[[`全表扫描`,f,f,`代价 = 读取行数`]]},[],[]),u){let e=o===`=`?1:Math.max(1,Math.min(l+1,d)),n=o===`=`?3:3+e;this._emit(`recurse-enter`,`候选计划 B：索引查找 —— B+ 树定位（代价 3）+ 读取命中行 `+e+` 行。`,{columns:[`计划`,`读取行数`,`估算代价`,`说明`],rows:[[`索引查找(`+i+`)`,e,n,`B+ 树定位 + 命中行`]]},[],[]);let r=n<f;this._emit(`compare`,`对比代价：全表扫描 `+f+` vs 索引查找 `+n+` —— `+(r?`索引查找胜出`:`全表扫描胜出（行数少时索引优势小）`),{columns:[`计划`,`读取行数`,`估算代价`,`结论`],rows:[[`全表扫描`,f,f,r?`放弃`:`✓ 选中`],[`索引查找(`+i+`)`,e,n,r?`✓ 选中`:`放弃`]]},[],[]),this._emit(`complete`,`优化器选择：`+(r?`索引查找`:`全表扫描`)+`。实际命中 `+l+` 行，结果如下。`,{columns:[...t],rows:c.map(e=>[...e])},[],[{type:`compare`,indices:c.map((e,t)=>t)}])}else this._emit(`compare`,`列「`+i+`」没有索引 —— 只有全表扫描一种候选计划。`,{columns:[`计划`,`读取行数`,`估算代价`,`结论`],rows:[[`全表扫描`,f,f,`✓ 选中`]]},[],[]),this._emit(`complete`,`执行全表扫描，实际命中 `+l+` 行。`,{columns:[...t],rows:c.map(e=>[...e])},[],[{type:`compare`,indices:c.map((e,t)=>t)}]);this.totalSteps=this.steps.length}_parseWhere(e){let t=e.match(/WHERE\s+([\u4e00-\u9fa5\w]+)\s*(>=|<=|<>|!=|=|>|<)\s*['"]?([\u4e00-\u9fa5\w.]+)['"]?/i);return t?{col:t[1],op:t[2],val:t[3]}:null}_evalCond(e,t,n){let r=typeof e==`number`?e:parseFloat(String(e)),i=parseFloat(n);if(!isNaN(r)&&!isNaN(i))switch(t){case`=`:return r===i;case`>=`:return r>=i;case`<=`:return r<=i;case`>`:return r>i;case`<`:return r<i;case`<>`:case`!=`:return r!==i}let a=String(e);switch(t){case`=`:return a===n;case`<>`:case`!=`:return a!==n;default:return!1}}_emit(e,t,n,r,i,a){this.steps.push({id:this._stepId++,type:e,description:t,data:r,highlights:i,pseudocodeLine:0,presenterNote:a,table:{columns:[...n.columns],rows:n.rows.map(e=>[...e])}})}},l=e(`<span>优化器为查询生成<b>候选执行计划</b>并估算代价：<span class="mono">全表扫描</span>按行读取， <span class="mono">索引查找</span>沿 B+ 树定位。等值/范围查询命中行少时索引胜出，
			选择性差的列则全表扫描更优。</span>`);function u(e,s){r(s,!0);let u={columns:[`学号`,`姓名`,`专业`,`成绩`],rows:[[20101,`张三`,`计算机`,88],[20102,`李四`,`软件工程`,92],[20103,`王五`,`计算机`,76],[20104,`赵六`,`网络工程`,85],[20105,`孙七`,`软件工程`,63]]},d=[`SELECT * FROM 学生 WHERE 学号 = 20103`,`SELECT * FROM 学生 WHERE 成绩 >= 85`,`SELECT * FROM 学生 WHERE 专业 = '计算机'`];function f(){let e=new c;return e.init({sql:d[0],table:u,indexedCols:[`学号`,`成绩`]}),e}let p=t(f());a(e,{sectionNum:`§13`,sectionName:`优化原理`,title:`执行计划与索引选择`,desc:e=>{var t=l();i(e,t)},children:(e,t)=>{o(e,{get engine(){return p},topicId:`explain-plan`,topicName:`执行计划与索引选择`})},$$slots:{desc:!0,default:!0}}),n()}export{u as component};