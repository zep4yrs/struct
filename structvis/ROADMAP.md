# StructVis 发展规划（Roadmap）

> 本文件记录已确认但**尚未实施**的课程/功能计划。实施时按批次顺序推进，每批完成后更新本文件（勾选 ✅）。
> 新增课程 = 在 `src/lib/content/topics.ts` 加一项 + 建页面 + 建引擎 + 图谱节点 + 导航 + 旁白音频全链路接入。

---

## 批次一（优先 · 高频考点）

| 主题 | 教学点 | 可视化亮点 |
|---|---|---|
| 动态规划 · 0-1 背包 | DP 核心思想：状态定义 + 转移方程 | 二维表格逐格填充，高亮依赖格 |
| 最长公共子序列 LCS | 递推表 + 回溯构造 | 表 + 箭头回溯路径 |
| 并查集 | 路径压缩 + 按秩合并 | 树形集合合并动画 |

## 批次二

| 主题 | 教学点 | 可视化亮点 |
|---|---|---|
| B 树 / B+ 树 | 多路平衡、分裂/合并（数据库索引核心） | 节点分裂动画，键提升 |
| MySQL 架构总览 | 连接器 → 解析器 → 优化器 → 执行器全链路 | 链路高亮 |
| 单调栈 | 单调性维护（接雨水/最大矩形） | 栈内元素高亮 + 丢弃动画 |

## 批次三

| 主题 | 教学点 |
|---|---|
| 强连通分量 Tarjan | 时间戳 + 低链接，DFS 树 + 栈内节点着色 |
| 最大流（Edmonds-Karp） | 增广路 + 残余网络，双向边流量/容量标注 |
| 二分图判定 | 染色法，交替染色冲突检测 |
| 跳表 Skip List | 多层索引、概率平衡，插入逐层下沉 |

## 批次四

| 主题 | 教学点 |
|---|---|
| BM / Sunday 字符串匹配 | 坏字符、好后缀跳跃 |
| LRU 缓存 | 哈希 + 双向链表 |
| 八皇后（回溯） | 回溯 + 剪枝棋盘 |
| 锁机制与死锁 | 行锁/间隙锁，死锁检测可视化 |
| InnoDB 日志体系 | redo/undo/binlog 三条链路 |
| 主从复制 | binlog 同步流程 |

---

## 竞速实验室 · 趣味算法跑道

在现有 8 条严肃跑道（快排/归并/冒泡/选择/插入/堆/希尔/基数）之外，增加"趣味算法"分组跑道：

| 算法 | 玩法 | 竞速效果 |
|---|---|---|
| 灭霸排序（Thanos Sort） | 随机删除一半元素；若仍无序继续删一半，直到有序或只剩 1 个 | 瞬间"减半"，结果随机、不保证有序 |
| 斯大林排序（Stalin Sort） | 从左到右扫描，逆序元素直接"枪毙"（删除），剩余天然有序 | O(n) 秒完成，但丢弃大量元素 |
| 猴子排序（Bogo Sort） | 随机打乱，检查有序？无序继续打乱 | 期望 O(n·n!)，几乎永远跑不完 |

设计要点：
- 与严肃算法同屏竞速，突出"复杂度 vs 正确性"的对照教学价值
- 引擎需保证步骤数有限（斯大林 = 一次扫描；灭霸 = 至多 log₂n 轮；猴子 = 打乱步数设上限并在 complete 文案说明"期望无限"）
- 跑道卡片加"趣味"徽标区分；完成后结果可能未排序（灭霸），需在结果区给出说明文案
